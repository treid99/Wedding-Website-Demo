node_modules/
.next/
.next-test/
out/
build/

# Source photos ARE committed (~10MB, 2048px WebP). They were shrunk from the
# 180MB full-resolution set by `npm run photos:shrink` so the repo is
# self-contained: clone, `npm run setup`, done.

# Generated photo derivatives + manifest — rebuilt by `npm run photos:build`
/public/photos/
/data/photos.json

# Local SQLite database
/data/*.db
/data/*.db-shm
/data/*.db-wal

# Test output — `npm run test:report` opens the last HTML report
/test-results/
/playwright-report/
/blob-report/

next-env.d.ts
*.tsbuildinfo
.env*
.DS_Store
